---
topic: Sway v1 architecture spine
altitude: initiative (keeps epics)
purpose: build-substrate
updated: 2026-08-23T03:39
---

- (override) Headless run per caller payload: architect + smart owner proxy; deliverable fixed at planning-artifacts/architecture.md; OQ-1..OQ-4 to be closed with rationale
- (constraint) Chain-top inputs bind: PRD (40 FR/10 NFR, C-1..C-8), UX spec (player transition <=300ms hard cap, zero blur v1, state quintet), lessons L1-L8, decisions 0001/0003
- (decision) AD-1 paradigm: hexagonal ports-and-adapters over a 7-module Android graph; UI never touches transport types; extractor visible only to :catalog
- (decision) OQ-1 CLOSED: v1 ships NewPipeExtractor-only behind our own CatalogSource + StreamResolver ports; InnerTube adapter slots in later without UI/repository changes; consequence: FR-3 Home Feed degrades to Search-first landing branch (UX already designed it) until InnerTube lands
- (decision) OQ-2 CLOSED: Hilt end-to-end (singletons + ViewModels + service); no Koin, no bridge — reference evidence shows Koin alone could not own heavy resource singletons and forced a HiltKoinBridge (L1 refuses bridges)
- (decision) OQ-3 CLOSED: OkHttp is the single HTTP stack — NewPipe downloader impl runs on it, Coil 3 uses coil-network-okhttp natively, one client config; Ktor/Cronet/Retrofit excluded from v1
- (decision) OQ-4 CLOSED (PROVISIONAL to release gate): personal-use framing until distribution review; NewPipe-only transport keeps posture simpler than InnerTube clients
- (assumption) Room 2.8.4 chosen over room3 3.0.0: room3 stable only since 2026-07-01 with new package + coroutine-only APIs; solo-dev risk calculus favors boring tech; DAO layer isolates a future upgrade
- (version) Pins verified on the web 2026-08-23: Kotlin 2.4.10, AGP 9.3.0 + Gradle 9.5.0, compileSdk 36 (AGP 9.3 supports 37), JDK 21, material3 1.4.0 stable (Expressive APIs stable in it), Compose ui/foundation 1.11.x, navigation-compose 2.9.x, Media3 1.11.0 (2026-08-05; StuckPlayerException detection incl.), Room 2.8.4, Hilt 2.60.1 + androidx.hilt 1.4.0, Coil 3.5.0, OkHttp 5.5.0, NewPipeExtractor v0.26.5 JitPack (2026-08-15), DataStore 1.2.x, coroutines 1.11.x, KSP matched to Kotlin
- (decision) Playback topology (C-3): SwayPlaybackService : MediaLibraryService owns ExoPlayer built in onCreate; UI talks through PlayerConnection facade over MediaController; queue built with Source-ID placeholder URIs except start item; service re-resolves placeholders on transition with single-flight guard so background advance works with zero UI bound
- (decision) StreamResolver port contract: resolveAudio(trackId, AudioRequest)->SwayResult<ResolvedAudio>, invalidate(trackId), prefetchNext(trackId, request) age-capped optional; ResolvedAudio carries url + expiresAtEpochMs parsed from URL's own expiry param (never a guessed TTL) + bitrateKbps + backendTag + rendition cacheKey
- (decision) Expiry defense collapsed to 3 layers + dedup (C-5): (1) read-time validation of cached/prefetched URLs against expire= param minus 5 min margin; (2) HTTP 403/410 -> invalidate + deduped fresh resolve + position resume within +/-3 s; (3) stuck-buffer watchdog >3 s retry/downscale, >15 s full rebuild, then skip + typed error; in-flight resolve coalescing mandatory; Media3 StuckPlayerException kept as builder-level backstop only
- (decision) Data layer: exactly one Room DB; entities Song/Playlist/PlaylistSong/History/QueueState; DAOs return Flows/suspend only; exportSchema=true from day one; explicit migrations only, destructive fallback refused (reference F4 lesson); history upsert-recency capped 500; QueueState stores song snapshots so restore renders offline
- (decision) Typed errors end-to-end (C-2/NFR-2): SwayResult<T> sealed union + SwayError taxonomy (Offline, RateLimited, UpstreamUnavailable, ContentNotFound-permanent, Parse, Storage, Unknown-caused); repositories never return emptyList-as-failure; failure->UI-state mapping table is the FR-37 contract
- (decision) Startup law (NFR-1): Application.onCreate does no disk/network/prefs work; splash gates on first composition, never data; Playback Session restore runs post-composition from Room and lands paused (FR-25); Search-first Home landing is offline-safe by construction
- (decision) Image pipeline: Coil 3.5.0 (memory ~25%, disk 256 MB LRU per A-6) fed ArtworkRef value objects normalized at parse time — ytimg hosts walk maxres->sd->hq->mq on 404, ggpht/googleusercontent get size params rewritten descending; canonical URL is the cache key; placeholder bounds identical to art (FR-36)
- (decision) Audio focus/route policy (FR-19/20): Media3 setAudioAttributes(handleAudioFocus=true) semantics — transient loss pauses and resumes on regain, ducking where system grants it, permanent loss stops; becoming-noisy pauses instantly; reconnect never auto-resumes
- (question) Carried runtime unknowns needing device evidence: googlevideo URL expiry/IP-binding envelope (tunes -5min margin + prefetch age cap); prevalence of ciphered formats on NewPipe primary path (if extractor stops yielding playable URLs, OQ-1 must be revisited early)
- (resolution) POST_NOTIFICATIONS-denied behavior of media FGS on 13+ RESOLVED at story 6.3 (2026-08-24): media-session notifications are platform-EXEMPT from the runtime permission (official docs notification-permission#exemptions-media-sessions; grounded in MediaNotificationManager 1.11.0 source posting under SuppressLint("MissingPermission")) — playback + lock-screen/media controls keep working when denied; residual risk is OEM-skin divergence only, kept as R-3 device-matrix item feeding story 15.3
- (event) Reviewer gate run non-interactively: lint_spine clean (0 findings); two configured lenses executed sequentially (no subagent tool available), full reviews written to planning-artifacts/reviews/
- (decision) Gate fixes applied: AD-8 single QueueSnapshot serializer owned by core:data; AD-6 placeholder scheme defined once in playback + history recorded service-side only; AD-7 AudioRequest/ResolvedAudio/quality enum pinned to core:model; AD-11 designui consumes artwork candidate chain as data, zero host logic
- (event) spine finalized at planning-artifacts/architecture.md (523 lines); status final; next chain step bmad-create-epics-and-stories
- (decision) OWNER 2026-08-24: violet brand RETIRED -> two-mode theme system in :designui: Ink & Paper monochrome default (Notion philosophy) + artwork-DYNAMIC mode (whole-app recolor from playing cover, SuvMusic-inspired engine cascade seed->scheme with spring transitions); semantic rose=like / amber=caution roles preserved; ThemeMode persistence lands 15.1
