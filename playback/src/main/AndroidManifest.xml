<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- AD-6/AR-5: SwayPlaybackService is the ONLY player owner.
         Declared here for library merging and for Robolectric test manifests;
         the :app manifest also declares it with foregroundServiceType for the final APK. -->
    <application>
        <service
            android:name="com.sway.playback.SwayPlaybackService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>
    </application>

</manifest>
